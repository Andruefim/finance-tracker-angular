import { ExpensesChartData } from './expenses-chart-data.model';

describe('ExpensesChartData', () => {
  it('should create an instance', () => {
    expect(new ExpensesChartData()).toBeTruthy();
  });
});
